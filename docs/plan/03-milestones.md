# Milestones

M0 — Bootstrap environment + Doom sync
M1 — Syntax-aware chunking
M2 — Embeddings + pgvector
M3 — Hybrid retrieval (interactive profile) + retrieval fixtures
M3b — Deep mode retrieval (multi-query + stronger rerank)
M4 — LLM answers + citations + streaming
M5 — Diff output (existing files only)
M6 — Minimal UI (profile toggle selects retrieval mode)
M7 — Railway deployment + manual ingest into Railway Postgres
M8 — Query interface enhancements (syntax highlighting, relevance scores, drill-down to full file)
M9 — Code understanding features (4+ from REQUIREMENTS)
M10 — Submission docs (Pre-Search, RAG Architecture, AI Cost analysis)
M11 — Diff in UI
M12 ✅ — Conversation context (multi-turn, history passed to LLM)
M13 ✅ — Evals (50 cases: happy/edge/adversarial/performance, local run, LangSmith upload, parallel)